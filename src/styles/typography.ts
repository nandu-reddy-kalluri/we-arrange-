/**
 * typography.ts
 * Semantic design tokens for typography sizing, families, and weights.
 */
export const typography = {
  heroTitle: "font-serif font-bold text-[clamp(36px,5vw,72px)] leading-[1.1] tracking-tight md:tracking-normal",
  heroSubtitle: "font-sans text-[clamp(16px,2vw,24px)] leading-[1.6]",
  sectionTitle: "font-serif font-bold text-[clamp(28px,4vw,48px)] leading-[1.15] tracking-tight",
  cardTitle: "font-serif font-bold text-[clamp(20px,2.5vw,32px)] leading-snug",
  cardDesc: "text-[clamp(14px,1.2vw,18px)] font-medium leading-relaxed text-neutral-muted",
  body: "text-[clamp(14px,1vw,16px)] leading-relaxed",
  label: "text-[clamp(12px,0.9vw,14px)] font-black uppercase tracking-wider",
  button: "text-[clamp(14px,1vw,16px)] font-black uppercase tracking-widest",
  navigation: "text-[clamp(14px,1vw,16px)] font-bold",
  footer: "text-[clamp(13px,1vw,15px)]",
  caption: "text-[clamp(11px,0.8vw,12px)] font-semibold text-neutral-muted"
};
