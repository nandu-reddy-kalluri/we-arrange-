/**
 * responsive.ts
 * Semantic design tokens for visibility and breakpoints.
 */
export const responsive = {
  mobileOnly: "block md:hidden",
  desktopOnly: "hidden md:block",
  tabletUp: "hidden sm:block",
  flexMobileStack: "flex flex-col md:flex-row",
  flexDesktopStack: "flex flex-row md:flex-col",
};
